// 服务配置
const serverConfig = { host: "localhost", port: 8888 }

// 连接数据库的参数
const databaseConfig = { host: "127.0.0.1", user: "root", password: "123456", database: "database" }

module.exports = { serverConfig, databaseConfig }
